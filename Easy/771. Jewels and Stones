class Solution(object):
    def numJewelsInStones(self, jewels, stones):
        value = 0
        for stone in stones:
            if stone in jewels:
                value += 1
        return value
        """
        :type jewels: str
        :type stones: str
        :rtype: int
        """
        
