class Solution(object):
    def finalValueAfterOperations(self, operations):
        value =0 
        for n in operations:
            if n == '--X' or n == 'X--':
                value -=1
            else: value += 1
        return value
        """
        :type operations: List[str]
        :rtype: int
        """
        
