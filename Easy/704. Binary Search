class Solution(object):
    def search(self, nums, target):
        for n in range(len(nums)):
            if nums[n] == target:
                return n
        return -1
        """
        :type nums: List[int]
        :type target: int
        :rtype: int
        """
        
